import React from "react";

/**
 * 
 * Displays all blogs and should allow the user to edit
 * 
 * @returns 
 */
const page = () => {
  return <section className="flex items-center justify-center h-screen w-full">Blog</section>;
};

export default page;
