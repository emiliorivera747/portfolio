import React from "react";

const loading = () => {
  return (
    <div className="flex item-center justify-center bg-black h-screen w-screen">
      Loading
    </div>
  );
};

export default loading;
