import React from 'react'

function TextTitleSection({paragraph, title, bgColor, titleColor, pColor}) {
  return (
    <div className={`${bgColor} min-h-screen  h-auto flex flex-col items-center jusify-center gap-4`}></div>
  )
}

export default TextTitleSection